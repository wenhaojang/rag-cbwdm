# TEMPLATE ONLY.
# Replace placeholders from:
# server_state_snapshots/<timestamp>/environments/rag-cbwdm-retrieval/
name: rag-cbwdm-retrieval
channels:
  - <CHANNELS_FROM_CAPTURED_FROM_HISTORY>
dependencies:
  - python=<PYTHON_VERSION_FROM_CAPTURE>
  - pip=<PIP_VERSION_FROM_CAPTURE>
  - openjdk=21
  # Copy every other explicitly requested conda dependency from the captured
  # conda-from-history.yml. Install the captured Pyserini 2.3.0 provenance
  # exactly as shown by conda-no-builds.yml/pip-freeze.txt.
  - pip:
      - pyserini==2.3.0
      - <OTHER_RETRIEVAL_PIP_REQUIREMENTS_FROM_CAPTURE>
